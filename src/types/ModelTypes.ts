import type { BattleData, BattleConditions } from '@/models/Battle.ts
import type { BattleMemberData, BattleMemberConditions } from '@/models/BattleMember.ts';
import type { BossData, BossConditions } from '@/models/Boss.ts';
import type { GuildBattleAlertData, GuildBattleAlertConditions } from '@/models/GuildBattleAlert.ts';
import type { GuildTeamRoleData, GuildTeamRole } from '@/models/GuildTeamRole.ts';
import type { MasterCPMData, MasterCPMConditions } from '@/models/MasterCPM.ts';
import type { MasterPokemonData, MasterPokemonConditions } from '@/models/MasterPokemon.ts';
import type { PogoHubLinkData, PogoHubLinkConditions } from '@/models/PogoHubLink.ts';
import type { TrainerData, TrainerConditions } from '@/models/Trainer.ts';
import type { TranslationData, TranslationConditions } from '@/models/Translation.js';
import type { WikiLinkData, WikiLinkConditions } from '@/models/WikiLink.ts';

export type {
    BattleData, BattleConditions,
    BattleMemberData, BattleMemberConditions,
    BossData, BossConditions,
    GuildBattleAlertData, GuildBattleAlertConditions,
    GuildTeamRoleData, GuildTeamRole,
    MasterCPMData, MasterCPMConditions,
    MasterPokemonData, MasterPokemonConditions,
    PogoHubLinkData, PogoHubLinkConditions,
    TrainerData, TrainerConditions,
    TranslationData, TranslationConditions,
    WikiLinkData, WikiLinkConditions
};